version: '3.1'
services:
  keycloak:
    image: quay.io/keycloak/keycloak:9.0.3
    restart: always
    ports:
      - "8080:8080"
    environment:
      KEYCLOAK_USER: admin
      KEYCLOAK_PASSWORD: admin